module.exports = {
  rules: {
    // ...
    "indent": ["warn", 2],

    // Commas
    "comma-spacing": ["warn"],
    "comma-dangle": ["warn", "never"],

    // Quotes
    "quotes": ["warn", "single"],

    // Spaces
    "key-spacing": ["warn"],
    "no-multi-spaces": ["warn"],
    "no-trailing-spaces": ["warn"]
  }
}